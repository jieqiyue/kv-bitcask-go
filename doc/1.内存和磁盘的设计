1.对于bitcask来说，内存的数据结构的选择很多，比如说BTree，跳表，红黑树。（天然有序）
2. lsm tree是一种文件。文件内容是已经排序好了的key/value键值对。这个排序是在内存中就排序好的，
内存里面使用一种排序的数据结构。然后存储到磁盘上面自然就是有序的。然后存到磁盘之后一般就不能修改了。
因为一般都是追加记录。
3.merge流程两个目标：
3.1 在merge的过程中，不要对线上的正常请求造成太大影响。
3.2 在merge之后，生成hint文件，加速数据库启动流程，防止启动的时候重放数据文件中的所有操作。

4. 数据清理流程

5. 锁相关。

6. 优化merge过程